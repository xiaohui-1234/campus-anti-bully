<view class="empty">
  <view class="empty-title">{{title}}</view>
  <view class="empty-desc">{{desc}}</view>
  <view wx:if="{{actionText}}" class="empty-action" bindtap="handleAction">{{actionText}}</view>
</view>
