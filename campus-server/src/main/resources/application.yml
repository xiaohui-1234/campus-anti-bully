server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: campus-server
  profiles:
    active: dev
  jackson:
    property-naming-strategy: SNAKE_CASE
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 3MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://39.105.153.170/campus?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: zhouyuhui
  data:
    redis:
      host: 47.116.107.124
      port: 6379
      password: zhouyuhui
      database: 0

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

campus:
  security:
    jwt:
      access-token-expire-seconds: 7200
      refresh-token-expire-seconds: 604800
      secret: "please-change-this-secret-at-least-32-bytes"
  wx:
    miniapp:
      app-id: "wx28559fd52a9bef53"
      app-secret: "d4379381a41173222cb7feba03fd9504"
  mqtt:
    enabled: true
    broker-url: "tcp://47.116.107.124:1883"
    client-id: "campus-server"
    username: "root"
    password: "@Zyh056610"
    qos: 1
    completion-timeout-ms: 5000
  minio:
    endpoint: "http://47.116.107.124:9000"
    access-key: "minioadmin"
    secret-key: "minioadmin123"
    bucket: "campus"
    public-endpoint: "https://oss-shanghai.1314-520.xyz"
  cache:
    mqtt-dedup-ttl-seconds: 10
    device-online-ttl-seconds: 90
    bind-code-ttl-seconds: 60
    access-url-ttl-seconds: 3600
    upload-url-ttl-seconds: 300
    jwt-blacklist-ttl-seconds: 7200
    unpulled-event-cache-ttl-seconds: 300
  storage:
    object-key-pattern: "{product_type}/audio/{device_id}/{yyyy}/{MM}/{dd}/{event_id}.{ext}"
    avatar-key-pattern: "avatar/{user_id}.{ext}"
    avatar-max-size-mb: 2
  event:
    default-page-size: 10
    max-page-size: 100
